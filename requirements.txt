# Solana Memecoin Trading Bot v0.2 - Dependencies
# Install with: pip install -r requirements.txt
# Compatible con Python 3.13 + Android ARM64 (sin compilación Rust/C)

# Core - Puro Python
python-dotenv>=1.0.0
colorlog>=6.8.0

# Async HTTP
aiohttp>=3.9.0
aiohttp-socks>=0.8.0

# Criptografía - Ed25519 para firma de transacciones (wheel Android disponible)
cryptography>=42.0.0

# Encoding
base58>=2.1.1

# HTTP sync/async alternativo
httpx>=0.28.0

# Testing (optional)
pytest>=7.4.0
pytest-asyncio>=0.21.0