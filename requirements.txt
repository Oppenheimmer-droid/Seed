# Solana Memecoin Trading Bot v2.1 - Dependencies
# Install with: pip install -r requirements.txt

# ✅ VERIFIED for Python 3.13 + Android ARM64 (no compilation needed)
aiohttp==3.14.1
aiohttp-socks==0.11.0
base58==2.1.1
python-dotenv==1.2.2
colorlog==6.10.1
httpx==0.28.1

# Optional: Try PyNaCl wheel first, fallback is ed25519_pure.py
# PyNaCl==1.5.0 --only-binary=:all:  # Optional, uses fallback if unavailable

# Optional: cryptography wheel (uses fallback if unavailable)
# cryptography --only-binary=:all:   # Optional, uses fallback if unavailable

# Testing (optional)
pytest>=7.4.0
pytest-asyncio>=0.21.0