# Solana Memecoin Trading Bot - Dependencies
# Install with: pip install -r requirements.txt

# Core
pydantic>=2.5.0
pydantic-settings>=2.1.0
python-dotenv>=1.0.0
colorlog>=6.8.0

# Async HTTP
aiohttp>=3.9.0
aiohttp-socks>=0.8.0

# Solana SDK (optional - for real trading)
# Install separately on Termux: pkg install rust cargo clang make
# Then: pip install solders solana base58
# solders>=0.18.0
# solana>=0.30.0
# base58>=2.1.1

# Testing (optional)
pytest>=7.4.0
pytest-asyncio>=0.21.0