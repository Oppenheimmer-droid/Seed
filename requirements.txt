# Solana Memecoin Trading Bot v2.0.0-ALPHA - Dependencies
# Install with: pip install -r requirements.txt

# Core
python-dotenv>=1.0.0
colorlog>=6.8.0

# Async HTTP (required for DexScreener API)
aiohttp>=3.9.0

# NOTE: cryptography, solders, PyNaCl are NOT required!
# This bot uses PURE PYTHON Ed25519 implementation
# Works on Python 3.13 + Android ARM64 without compilation

# Optional: If you want faster Ed25519, install cryptography (requires Rust)
# pip install cryptography  # Only if you have Rust installed

# Testing (optional)
pytest>=7.4.0
pytest-asyncio>=0.21.0